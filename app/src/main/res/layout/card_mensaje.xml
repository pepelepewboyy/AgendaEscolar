<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="12dp"
    app:cardCornerRadius="16dp"
    app:cardElevation="0dp"
    app:cardBackgroundColor="#FFFFFF">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- FILA SUPERIOR: icono + título + badge + punto azul -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <!-- Icono -->
            <ImageView
                android:layout_width="20dp"
                android:layout_height="20dp"
                android:src="@android:drawable/ic_dialog_info"
                android:layout_marginEnd="8dp" />

            <!-- Título -->
            <TextView
                android:id="@+id/txtTituloMensaje"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:fontFamily="@font/alatsi"
                android:text="Título del mensaje"
                android:textSize="14sp" />

            <!-- Badge -->
            <TextView
                android:id="@+id/txtBadgeMensaje"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="8dp"
                android:background="@drawable/bg_input_rounded"
                android:paddingHorizontal="10dp"
                android:paddingVertical="2dp"
                android:text="Importante"
                android:textSize="11sp"
                android:fontFamily="@font/advent_pro" />

            <!-- Punto azul de no leído -->
            <View
                android:id="@+id/indicadorNoLeido"
                android:layout_width="8dp"
                android:layout_height="8dp"
                android:layout_marginStart="8dp"
                android:background="@android:color/holo_blue_dark" />

        </LinearLayout>

        <!-- DESCRIPCIÓN -->
        <TextView
            android:id="@+id/txtDescripcionMensaje"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:fontFamily="@font/advent_pro"
            android:text="Descripción del mensaje..."
            android:textSize="13sp"
            android:maxLines="3"
            android:ellipsize="end" />

        <!-- FECHA Y HORA -->
        <TextView
            android:id="@+id/txtFechaMensaje"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:fontFamily="@font/advent_pro"
            android:text="Lunes, 1 de enero de 2025  8:00"
            android:textSize="11sp"
            android:gravity="end" />

    </LinearLayout>

</androidx.cardview.widget.CardView>